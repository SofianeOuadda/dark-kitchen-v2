{
  "name": "dark-kitchen-project",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run serve",
    "backend": "cd backend && nodemon server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
